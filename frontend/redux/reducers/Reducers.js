// combineReducers: Là một hàm từ thư viện Redux,
// được sử dụng để kết hợp nhiều reducers thành một
// reducer duy nhất để tạo ra một Redux store.

import { combineReducers } from "redux";

const Reducers = combineReducers({});

export default Reducers;
